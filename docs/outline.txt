CLIENT SIDE:

query flickr api, get an array of json objects

user picks one & clicks save

take that json object

serialize it 

send it to php

SERVER (PHP) SIDE:

listen for incoming json object submissions, store them

listen for incoming json object requests, return them



